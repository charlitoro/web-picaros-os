version: '3'
services:
    node-picaros:
        build: ./
        container_name: docker-node
        command: npm start
        ports:
            - 5500:5500
        volumes:
            - ./:/usr/app/
            - /usr/app/node_modules
    
